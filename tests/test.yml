---
- hosts: all
  remote_user: root
  become: yes

  vars:
    debug: true
    java_version: "8u66"

  roles:
    - role: silpion.java
      oracle_java_distribution: "srv"
      oracle_java_version: "{{ java_version }}"

    - rastandy.libreoffice-server
