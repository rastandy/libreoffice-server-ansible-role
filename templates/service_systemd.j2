[Unit]
Description=LibreOffice server

[Service]
User={{ libreoffice_user }}
Restart=on-failure

ExecStart=/bin/libreoffice "--accept=socket,host={{ libreoffice_listen_address }},port={{ libreoffice_listen_port }},tcpNoDelay=1;urp;" --headless --nodefault --nofirststartwizard --nolockcheck --nologo --norestore
ExecStop=pkill libreoffice

[Install]
WantedBy=multi-user.target
